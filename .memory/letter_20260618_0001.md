# Letter to Myself (Session Handoff)

**Date:** 2026-06-18 · Repo: `/Users/oliverbaer/Projects/minicms` · Branch: `main`

## 1. Executive Summary
* **Goal:** Ship the **Events module (Spec 2)** for the e-Ventschau festival site, then a **polish pass** (rebrand cleanup, colors, homepage bands, meta titles).
* **Current Status:** ✅ **DONE & deployed to production.** Events module merged (PR #1) and live; all four follow-up polish requests done and verified live. Nothing in-flight.

## 2. The "Done" List (Context Anchor)
* **Events module (24 tasks)** — built via subagent-driven-development + parallel Workflow authoring. Models `Event`/`Stage`/`Appearance`/`PriceTier` (`prisma/schema.prisma`); `src/lib/{slug,event-validation,events,admin-auth}.ts`; full API under `src/app/api/admin/events/**` (collection + `[id]` + granular `stages`/`appearances` sub-resources; auth+IDOR+XOR+tier-replace-all-`$transaction`); public `/events`, `/events/[slug]` (responsive `EventTimetable`), `EventCard`; admin list/new/edit + `StageManager`/`TimetableBuilder`; homepage line-up coupled to featured event; `/programm-2026` → 308 redirect; sitemap; `buildEventJsonLd` (MusicEvent). Idempotent seed of **e-Ventschau 2026** (2 stages, 10 appearances, 4 tiers).
* **Adversarial review caught + fixed 2 data-loss bugs**: TimetableBuilder dropped `note` on every blur; admin edit could blank title/desc/tiers if saved before load. Fixed (`loaded` guard, `res.ok` checks, note preserved).
* **Timezone correctness:** all dates/times render in `Europe/Berlin`; seed stores `+02:00` instants. Done in `EventTimetable.tsx`, `EventCard.tsx`, detail page, kuenstler Auftritte.
* **Artist hero photos:** all 7 bands now have `heroImage` from `e-ventschau.de/wp-content/...` (verified HTTP 200). Set in seed + live DB.
* **Polish pass (this session's 2nd half):**
  - **"Das Messer" removed everywhere** — rewrote `src/lib/ai-prompts.ts` for festival context; fixed skeleton defaults; DB migration cleaned `/informationen` metaTitle + `/foerderer` knife-FAQ. 0 "Messer" left in DB.
  - **Colors → original site's blue** — rewrote the `eventschau` preset in `src/lib/themes.ts` to `#006799`/`#2ea3f2` (was teal/gold); synced `siteSettings` colors + seed.
  - **Homepage bands** — line-up now uses `ArtistCard` (real photos) instead of text-only cards.
  - **Events linked** — added to mobile bottom quick-bar in `HeaderClient.tsx` (was already in desktop nav + overlay).
  - **Meta-title double-brand fix** — page-title template (`src/app/layout.tsx:37`) already appends `| <siteName>`; removed the redundant `– e-Ventschau` from 5 public page titles + made the AI prompt generate **suffix-free** titles.

## 3. The "Pain" Log (CRITICAL)
* **`curl` intermittently "command not found"** in the Bash tool unless the command is prefixed with `cd /Users/oliverbaer/Projects/minicms &&`. **Workaround:** use `node -e 'fetch(...)'` for HTTP checks (Node 26, global fetch) — reliable. Prefer this for live smoke tests.
* **`npx tsx -e "..."` fails on top-level `await`** ("Top-level await not supported with cjs"). **Workaround:** wrap in `async function main(){...}; main()`.
* **WebFetch summarizer can't extract WordPress CSS colors.** **Workaround:** `node -e` fetch the raw HTML and regex `#[0-9a-fA-F]{6}` / `--wp--preset--color` / stylesheet `<link>`s yourself. (e-ventschau.de = MrsDivi/Divi theme → blue `#2ea3f2`/`#006799`.)
* **Task-notification agent-id↔content mapping was unreliable** (implementer notifications re-surfaced with reviewer verdicts). **Workaround:** ground-truth with `git log`/`tsc`/`build` instead of trusting the notification attribution.
* **A migration can be out-of-date by the time it runs** — the first "Das Messer" migration missed `/informationen` because the page was re-saved (with the old AI value) after the migration. **Note:** re-scan the DB after fixes; don't assume one pass is complete.
* No unresolved failures. Everything builds (`tsc --noEmit` + `npm run build`) and is deployed.

## 4. Active Variable State
* **Branch `main`** @ `1678f90` (pushed). PR #1 merged. Remote: `git@github.com:nyxCore-Systems/minicms.git` (gh user `mrwind-up-bird`). Git author MUST be `oliver.baer@gmail.com`.
* **Vercel project `e-ventschau`** (`prj_P9QXGLlt0ohwNQHqXLFiH4GO6T9g`, org `team_YgDpjOPZILXct53IZD6xbM4p`). Deploy: `cd minicms && vercel --prod --yes`. Auth = `oliverbaer-1646`.
* **Live URL:** `https://project-b9ghb.vercel.app` (stable alias). **NOT yet on the `e-ventschau.de` custom domain** — that still serves the old WordPress site.
* **No Cloudinary creds locally** (`.env`/`.env.local` lack `CLOUDINARY_*`; they're Vercel-only). Can't upload to Cloudinary from here.
* **Theme/colors:** driven by `src/lib/themes.ts` `eventschau` preset (injected as CSS vars by `src/app/layout.tsx`), NOT `globals.css :root` (those are overridden) and NOT `siteSettings.primaryColor` (stored but template uses the preset).
* No test framework — verify with `npx tsc --noEmit` + `npm run build` + `npx tsx` for the pure-validator tests in `src/lib/__tests__/`.

## 5. Immediate Next Steps
1. [ ] **Re-host the 7 artist photos to Cloudinary BEFORE any cutover to the `e-ventschau.de` domain** — they currently point at the WordPress site's `/wp-content/`; cutover would break them. (Admin edit also nulls non-Cloudinary `heroImage` via `safeCloudinaryUrl`, so re-uploading via the admin MediaPicker is the proper path.)
2. [ ] Optional: pick a real **headliner/collage hero image** for the homepage (hero is currently text + the band-photo grid below it).
3. [ ] Optional: decide whether the nav still needs **both** "Programm 2026" (→ festival detail) and "Events" (→ list) — slight redundancy.
4. [ ] Optional: re-run **KI generieren** on `/informationen` so its meta title is regenerated suffix-free by the corrected prompt (currently the stored title still reads "Über uns - e-Ventschau Benefiz-Festival | e-Ventschau" — acceptable, just descriptive-name + brand).
5. [ ] Optional batch (Spec 2 §13, never done): Slider `EVENT` parity (`SliderItemType.EVENT` + `SliderItem.eventId` + branches in `sliders.ts` / `api/sliders`).
