# Letter to Myself (Session Handoff)

**Date:** 2026-06-19 14:11 CEST

## 1. Executive Summary
* **Goal:** Fix the e-Ventschau site (minicms, Next.js 15 / Vercel) — mobile menu had a transparent backdrop, and place the festival logo (background removed) where the hero's yellow glow circle was. Plus repo housekeeping (CLAUDE.md, tests, CI).
* **Current Status:** All done and verified live on `e-ventschau.vercel.app`. No work in progress.

## 2. The "Done" List (Context Anchor)
* **CLAUDE.md** rewritten to match current architecture (multi-model CMS, subdomain tenants, Plate directive system, AI, themes). Added `npm test` script (`tsx --test src/lib/__tests__/*.test.ts`) + a `Test` step in `.github/workflows/ci.yml`; bumped CI actions to v5 / Node 22 (silenced Node 20 deprecation).
* **Brand-color opacity bug (systemic):** brand colors were `'var(--brand-x)'`, so Tailwind `/opacity` modifiers emitted NO CSS (~190 dead utilities incl. mobile menu backdrop `bg-brand-bg/95` → transparent). Fixed in `tailwind.config.ts` via a `brandColor()` helper returning `color-mix(in srgb, var(--brand-x) calc(<alpha>*100%), transparent)`, typed as `string`. Mobile nav backdrop in `src/components/layout/HeaderClient.tsx` set to opaque `bg-brand-bg`.
* **Hero logo:** processed `~/.claude/image-cache/.../3.png` with `magick -fuzz 6% -transparent "#509bba" -trim` → `public/e-ventschau-logo.png` (transparent bg, origami birds preserved). Replaced `.nh-sun` glow circle with `<Image>` (`.nh-logo`) in `src/app/(public)/page.tsx` + `src/app/globals.css`.
* **Commits on `main`:** 9eb3f0c, 6070622 (color-mix + type fix), 9c9de90 (CI node bump), cc1b7fc (logo). All CI green.
* Wrote 2 project memories (Vercel domain gotcha, color-mix rule) under the `memory/` dir + MEMORY.md.

## 3. The "Pain" Log (CRITICAL)
* **Tried:** `bg-brand-bg/95` opacity modifier on a `var()` color. **Failed:** Tailwind silently emits no CSS for `/opacity` on var() colors → transparent. **Workaround:** `color-mix` in the config. Do NOT revert brand colors to plain `var()`.
* **Tried:** Fuzz 10–14% for logo background removal. **Failed:** eroded the light-blue origami birds. **Workaround:** fuzz 6% (birds intact, minor specks invisible at display size).
* **BIGGEST trap:** `git push` to `main` ran GitHub Actions (build only) but the live URL `e-ventschau.vercel.app` looked stale → wasted a debug cycle re-checking correct code. **Root cause:** `e-ventschau.vercel.app` was a *manual deployment-level alias* pinned to a 5-day-old deployment, while real production (`e-ventschau-oliver-baers-projects.vercel.app`) updated fine. **Fix:** `vercel domains add e-ventschau.vercel.app` (single arg, in linked dir) → now a project production domain that auto-tracks latest. *Note: when a fix "isn't live", VERIFY the deployment (curl asset status, grep live HTML/CSS, `vercel inspect <domain>`) before editing code again.*

## 4. Active Variable State
* No env/config changes this session. ESLint is NOT configured (`next lint` prompts interactively) — don't rely on `npm run lint`. `tsc --noEmit` and `npm test` both pass.
* `tailwind.config.ts` `brandColor()` returns a function cast to `string` (Config color type only allows string — required, do not "clean up").
* Untracked in working tree: `.memory/`, `.vscode/` (left unstaged intentionally).

## 5. Immediate Next Steps
1. [ ] User to hard-refresh `e-ventschau.vercel.app` (browser/CDN cache) to confirm logo + opaque menu visually on mobile/tablet.
2. [ ] Optional: reduce duplication — hero `<h1>` "e-Ventschau" now repeats the logo text; could shrink/hide `<h1>` if it looks doubled.
3. [ ] Optional: the logo's dark-navy crowd silhouettes vanish on the dark hero; consider a lighter logo variant or subtle backdrop if more contrast is wanted.
4. [ ] Optional: `vibe_publisher.yml` still uses `setup-python@v5` (Node 20-based) — bump to v6 if you want that workflow's deprecation warning gone too.
