# Letter to Myself (Session Handoff)

**Date:** 2026-06-15 · Repo: `/Users/oliverbaer/Projects/minicms` · Branch: `feat/events-module`

## 1. Executive Summary
* **Goal:** Spec 2 — the **Events module** for e-Ventschau (multistage timetable, artist-or-free-text appearance slots via role-as-edge, display-only price tiers, homepage line-up coupled to the main event).
* **Current Status:** ✅ **Design spec written, self-reviewed, committed.** Sitting at the **brainstorming user-review gate** — waiting for the user to approve the spec file before invoking `writing-plans`. NO code/schema written yet.

## 2. The "Done" List (Context Anchor)
* **Brainstorming complete** (5 locked decisions via 2 AskUserQuestion rounds): (1) Event = first-class listable entity; (2) timetable = multistage + real clock times; (3) price tiers = category + time-window, display-only; (4) slot = `artistId?` XOR `title`; (5) **homepage line-up coupled to main event** (the ambitious option).
* **Spec written:** `docs/superpowers/specs/2026-06-15-e-ventschau-events-module-design.md` (282 lines). Models: `Event`, `Stage` (per-event, cascade), `Appearance` (role-as-edge, `headliner|support|guest|break`), `PriceTier`. Public `/events` + `/events/[slug]` (responsive timetable: desktop grid Bühne×Zeit + day-tabs, mobile agenda-list — ASCII mockups in §7). Wires up the stubbed Artist "Auftritte" section. §14 carries every Spec 1 hardening lesson.
* **Committed:** `5c755ab` on new branch `feat/events-module` (branched off `ce2284d` main). Author = oliver.baer@gmail.com.
* **Self-review fix:** reconciled `eventType` contradiction — stored as `String` (no Prisma enum), validated against an allowlist in `event-validation`.
* **MCP gate honored:** consulted nyxcore project/personas/search before designing (search index mostly returns CLAUDE.md axiom chunks; the role-as-edge decision was already in working context).

## 3. The "Pain" Log (CRITICAL)
* **`/programm-2026` is NOT just a homepage link.** It's a markdown DB Page (`src/content/programm-2026.md`) referenced in FOUR places: `(public)/page.tsx` ×2, `components/layout/HeaderClient.tsx:94`, `components/layout/Footer.tsx:26`. Spec plan: make `/events/[slug]` canonical, repoint all 4, convert `/programm-2026` to a `redirect()`. **Don't naively delete it.**
* **Carry-over Spec 1 pains (do NOT relearn):** `npm run lint` UNUSABLE → use `npx tsc --noEmit` + `npm run build`. Plate imports: `TElement` from `@udecode/plate` (NOT -common); serialization from individual files `@/components/admin/editor/serialization/{markdownToPlate,plateToMarkdown}`; `PlateEditor` dynamic `.then(m => ({ default: m.PlateEditor }))`; `useParams()` not `use(params)`. JSON-LD must use `safeJsonLd`. Markdown rendered WITHOUT rehype-raw. Use `@@unique([tenantId, slug])` (scoped — NOT Vendor's global `@unique` bug). Prisma reads `.env` not `.env.local`.
* **DRY note:** plan extracts shared slug/URL validators (`SLUG_RE, normalizeSlug, isValidSlug, safeHttpsUrl, safeCloudinaryUrl`) from `artist-validation.ts` → new `src/lib/slug.ts`, **re-exported from artist-validation for zero breakage**. The umlaut-before-NFKD order MUST be preserved verbatim (`Motörhead→motoerhead`).
* No major failures this session.

## 4. Active Variable State
* **Branch:** `feat/events-module` @ `5c755ab` (only the spec doc committed). Main = `ce2284d`. Working tree: `.memory/` untracked (this letter).
* **Vercel/Neon/login creds:** unchanged from `letter_20260615_0001.md` (Vercel `prj_P9QXGLlt0ohwNQHqXLFiH4GO6T9g`; admin@e-ventschau.de / admin2024!).
* **nyxcore project:** `b9bb270f-c8f9-4fa2-8b62-7ecd7cbe5043` (minicms), 37 insights. Personas: Athena (data model), Harmonia (timetable a11y), Nemesis/Ipcha (later stress-test).
* **7 artist slugs (for appearance seed):** thorbjorn-risager, lebron-johnson, killabeatmaker, jed-thomas-band, rovar, nanny-goats, the-klaxon.

## 5. Immediate Next Steps
1. [ ] **Await user approval** of the spec. If changes requested → edit + re-run spec self-review loop.
2. [ ] On approval → invoke **`superpowers:writing-plans`** to produce the task-by-task plan at `docs/superpowers/plans/2026-06-15-events-modul.md`, following the §15 build order (schema → slug-extract → validation+lib → API → public → admin → homepage-coupling → seed → optional slider ⑥).
3. [ ] Then execute via **subagent-driven-development** (or Workflow, ultracode mode active) with adversarial multi-lens verification — same harness as the artist module.
4. [ ] **PENDING (agreed for right after spec lock):** add **hero photos for the 7 artists** — fetch official band photos from e-ventschau.de → upload to Cloudinary (only `res.cloudinary.com` renders via next/image) → set `heroImage` in seed + live DB. Currently all `heroImage = null` (♪ placeholder).
5. [ ] Open spec decisions still flagged for user: ④ /programm-2026 redirect+repoint, ⑤ granular vs fat endpoints, ⑥ keep/drop slider EVENT parity.
