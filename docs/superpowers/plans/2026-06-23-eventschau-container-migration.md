# e-Ventschau Container Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Containerize the e-Ventschau Next.js app and run it on the nyxCore server under `https://e-ventschau.nyxcore.cloud`, replacing the Vercel deployment, with data on a dedicated `eventschau` DB on the shared Postgres and media staying on Cloudinary.

**Architecture:** Mirror the proven `nyxcore-landingpage` deployment: a standalone Next.js build in a multi-stage Docker image, published by Traefik on the shared `nyxcore_nyxcore-net` network, deployed via GitHub Actions (rsync the source tree to `/opt/e-ventschau`, then `docker compose up -d --build`, then `prisma migrate deploy`, then health-check). Schema is baselined into Prisma migrations so `migrate deploy` is the going-forward mechanism.

**Tech Stack:** Next.js 15 (standalone output), Prisma 6 (PostgreSQL), NextAuth 4, Cloudinary, Docker + docker compose, Traefik, GitHub Actions.

## Global Constraints

- Node base image: `node:20-alpine` (matches landingpage; CI uses Node 22 — both fine for this app).
- Container/runtime port: `3000`; `HOSTNAME=0.0.0.0`.
- Docker network: external `nyxcore_nyxcore-net` (compose key `nyxcore-net`).
- Server deploy path: `/opt/e-ventschau` on `root@46.224.105.254` (`nyxcore.cloud`).
- Public hostname Phase 1: `e-ventschau.nyxcore.cloud`. `e-ventschau.de` added in Phase 2 (out of scope here).
- Tenant override: `TENANT_SLUG=rd-e-ventschau` (slug ≠ subdomain).
- `NEXTAUTH_URL=https://e-ventschau.nyxcore.cloud` (Phase 1).
- Secrets (`.env`, DB password) live ONLY on the host, never in the repo; rsync excludes `.env`.
- Media stays on Cloudinary — no media code changes.
- DB: dedicated `eventschau` DB + role `eventschau_app` on shared `postgres:5432`.
- All changes in the `nyxCore-Systems/minicms` repo, branch `feat/container-migration-nyxcore`.

---

### Task 1: Standalone build output + Prisma baseline migration

**Files:**
- Modify: `next.config.ts`
- Create: `prisma/migrations/0_init/migration.sql`
- Create: `prisma/migrations/migration_lock.toml`

**Interfaces:**
- Produces: a standalone build at `.next/standalone/server.js`; a Prisma migration `0_init` that the Dockerfile copies and `prisma migrate deploy` consumes.

- [ ] **Step 1: Add standalone output to `next.config.ts`**

Add the `output` key to the config object (top-level, alongside `images`):

```typescript
const nextConfig: NextConfig = {
  output: 'standalone',
  images: {
    remotePatterns: [
```

(Leave the rest of the file unchanged.)

- [ ] **Step 2: Generate the baseline migration from the current schema**

Run:

```bash
mkdir -p prisma/migrations/0_init
npx prisma migrate diff \
  --from-empty \
  --to-schema-datamodel prisma/schema.prisma \
  --script > prisma/migrations/0_init/migration.sql
```

Expected: `prisma/migrations/0_init/migration.sql` is created and is non-trivial (contains `CREATE TABLE` statements). Verify:

```bash
grep -c 'CREATE TABLE' prisma/migrations/0_init/migration.sql
```

Expected: a number > 10 (the schema has many models).

- [ ] **Step 3: Create the migration lock file**

Create `prisma/migrations/migration_lock.toml`:

```toml
# Please do not edit this file manually
# It should be added in your version-control system (e.g., Git)
provider = "postgresql"
```

- [ ] **Step 4: Verify the build produces a standalone server**

Run (a real or placeholder DB URL is fine — pages are `force-dynamic`, so no DB is hit at build):

```bash
DATABASE_URL='postgresql://placeholder:placeholder@localhost:5432/placeholder' \
DATABASE_URL_UNPOOLED='postgresql://placeholder:placeholder@localhost:5432/placeholder' \
NEXTAUTH_SECRET='placeholder-build-secret' \
NEXTAUTH_URL='https://e-ventschau.nyxcore.cloud' \
TENANT_SLUG='rd-e-ventschau' \
npm run build
```

Expected: build succeeds; `.next/standalone/server.js` exists:

```bash
test -f .next/standalone/server.js && echo "standalone OK"
```

Expected output: `standalone OK`

- [ ] **Step 5: Verify tests still pass**

Run:

```bash
npm test
```

Expected: PASS (`slug.test.ts`, `event-validation.test.ts`).

- [ ] **Step 6: Commit**

```bash
git add next.config.ts prisma/migrations
git commit -m "feat: standalone build output + Prisma baseline migration (0_init)"
```

---

### Task 2: Dockerfile + .dockerignore

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`

**Interfaces:**
- Consumes: standalone build (Task 1), `prisma/migrations/0_init` (Task 1).
- Produces: image `e-ventschau:latest` exposing port 3000 with a `/`-based HEALTHCHECK; ships the Prisma CLI + engines so `prisma migrate deploy` runs inside the container.

- [ ] **Step 1: Create `.dockerignore`**

```
node_modules
.next
out
.git
.github
.env
.env.local
.env.*.local
.vercel
.DS_Store
npm-debug.log*
yarn-error.log*
README.md
Dockerfile
docker-compose.yml
.dockerignore
.gitignore
*.tsbuildinfo
next-env.d.ts
drafts
docs
```

- [ ] **Step 2: Create `Dockerfile`**

```dockerfile
# syntax=docker/dockerfile:1.7
# e-Ventschau — Next.js 15 standalone build, served from node:20-alpine.
# Multi-stage: deps -> builder -> runner. Prisma CLI + engines are copied into
# the runner so `prisma migrate deploy` can run inside the container.

# 1. deps — install node_modules once, cache until package-lock changes.
FROM node:20-alpine AS deps
WORKDIR /app
RUN apk add --no-cache libc6-compat
COPY package.json package-lock.json* ./
RUN --mount=type=cache,target=/root/.npm \
    npm ci --prefer-offline --no-audit --fund=false

# 2. builder — produce .next/standalone + .next/static
FROM node:20-alpine AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .

# Pages are force-dynamic, so the build does not touch the DB. Placeholders keep
# any module that reads these at import time from throwing during `next build`.
ENV NEXT_TELEMETRY_DISABLED=1 \
    NODE_ENV=production \
    DATABASE_URL=postgresql://placeholder:placeholder@localhost:5432/placeholder \
    DATABASE_URL_UNPOOLED=postgresql://placeholder:placeholder@localhost:5432/placeholder \
    NEXTAUTH_SECRET=placeholder-build-secret \
    NEXTAUTH_URL=https://e-ventschau.nyxcore.cloud \
    TENANT_SLUG=rd-e-ventschau

RUN npm run build

# 3. runner — minimal production image
FROM node:20-alpine AS runner
WORKDIR /app

ENV NODE_ENV=production \
    NEXT_TELEMETRY_DISABLED=1 \
    PORT=3000 \
    HOSTNAME=0.0.0.0

RUN addgroup --system --gid 1001 nodejs \
    && adduser --system --uid 1001 nextjs

COPY --from=builder --chown=nextjs:nodejs /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

# Prisma schema + migrations + CLI + engines so `prisma migrate deploy` runs
# in-container (the standalone trace includes @prisma/client but NOT the CLI).
COPY --from=builder --chown=nextjs:nodejs /app/prisma ./prisma
COPY --from=builder --chown=nextjs:nodejs /app/node_modules/prisma ./node_modules/prisma
COPY --from=builder --chown=nextjs:nodejs /app/node_modules/@prisma/engines ./node_modules/@prisma/engines
COPY --from=builder --chown=nextjs:nodejs /app/node_modules/.bin/prisma ./node_modules/.bin/prisma

USER nextjs

EXPOSE 3000

HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=3 \
    CMD wget -qO- http://127.0.0.1:3000/ > /dev/null || exit 1

CMD ["node", "server.js"]
```

- [ ] **Step 3: Build the image to verify it compiles**

Run:

```bash
docker build -t e-ventschau:latest .
```

Expected: build completes without error; final stage tagged `e-ventschau:latest`.

- [ ] **Step 4: Verify the Prisma CLI is present in the image**

Run:

```bash
docker run --rm --entrypoint sh e-ventschau:latest -c './node_modules/.bin/prisma --version'
```

Expected: prints Prisma CLI / engine versions (no "not found" error).

- [ ] **Step 5: Commit**

```bash
git add Dockerfile .dockerignore
git commit -m "feat: multi-stage Dockerfile + dockerignore (standalone + prisma CLI)"
```

---

### Task 3: docker-compose.yml (Traefik + shared network)

**Files:**
- Create: `docker-compose.yml`

**Interfaces:**
- Consumes: image built from the `Dockerfile` (Task 2); host `.env` (Task 5/cutover).
- Produces: service `eventschau` (container `e-ventschau`) reachable by Traefik on `e-ventschau.nyxcore.cloud`.

- [ ] **Step 1: Create `docker-compose.yml`**

```yaml
# e-Ventschau — standalone service on the shared nyxcore_nyxcore-net network so
# Traefik auto-discovers it. Data lives in the dedicated `eventschau` Postgres DB
# (shared instance); media stays on Cloudinary. No local volume needed.
#
# Deploy path: /opt/e-ventschau on root@46.224.105.254
# Secrets come from /opt/e-ventschau/.env (NOT in the repo).

services:
  eventschau:
    build:
      context: .
      dockerfile: Dockerfile
    image: e-ventschau:latest
    container_name: e-ventschau
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
    networks:
      - nyxcore-net
    labels:
      traefik.enable: "true"
      traefik.http.routers.eventschau.rule: "Host(`e-ventschau.nyxcore.cloud`)"
      traefik.http.routers.eventschau.entrypoints: "websecure"
      traefik.http.routers.eventschau.tls.certresolver: "letsencrypt"
      traefik.http.routers.eventschau.middlewares: "security-headers@file,gzip-compress@file"
      traefik.http.routers.eventschau.service: "eventschau-service"
      traefik.http.services.eventschau-service.loadbalancer.server.port: "3000"
      traefik.http.services.eventschau-service.loadbalancer.server.scheme: "http"

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.1"
          memory: 128M

networks:
  # Join the existing compose-managed network from /opt/nyxcore.
  # docker prefixes project names: "nyxcore" -> "nyxcore_nyxcore-net".
  nyxcore-net:
    external: true
    name: nyxcore_nyxcore-net
```

- [ ] **Step 2: Validate the compose file**

Create a throwaway `.env` so `env_file` resolves during validation, then validate:

```bash
printf 'DATABASE_URL=postgresql://x:x@postgres:5432/eventschau\n' > .env.validate
docker compose --env-file .env.validate -f docker-compose.yml config >/dev/null && echo "compose OK"
rm -f .env.validate
```

Expected output: `compose OK` (the `env_file: - .env` may warn if `.env` is absent locally; if so, `touch .env` first and remove it after — `.env` must never be committed).

- [ ] **Step 3: Commit**

```bash
git add docker-compose.yml
git commit -m "feat: docker-compose with Traefik labels for e-ventschau.nyxcore.cloud"
```

---

### Task 4: Database provisioning SQL

**Files:**
- Create: `db/provision.sql`

**Interfaces:**
- Produces: role `eventschau_app` + database `eventschau` on the shared Postgres; consumed once during cutover (Task 7).

- [ ] **Step 1: Create `db/provision.sql`**

```sql
-- provision.sql — one-time setup, run on the server as the Postgres superuser.
--
-- Creates a DEDICATED database + restricted role for e-Ventschau so the
-- public-facing container never holds credentials to the core `nyxcore` DB.
-- Run once (from /opt/nyxcore where the platform compose lives):
--
--   docker compose exec -T postgres psql -U nyxcore -d postgres < provision.sql
--
-- Then set DATABASE_URL for the e-ventschau container (.env):
--   postgresql://eventschau_app:<password>@postgres:5432/eventschau

-- 1. Role (change the password before running).
DO $$
BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'eventschau_app') THEN
    CREATE ROLE eventschau_app LOGIN PASSWORD 'CHANGE_ME_strong_password';
  END IF;
END
$$;

-- 2. Dedicated database owned by the app role.
--    (CREATE DATABASE can't run inside a transaction; if it already exists
--    psql warns and continues — that's fine.)
\set ON_ERROR_STOP off
CREATE DATABASE eventschau OWNER eventschau_app;
\set ON_ERROR_STOP on

-- 3. Lock the role to its own database only.
REVOKE ALL ON DATABASE eventschau FROM PUBLIC;
GRANT CONNECT ON DATABASE eventschau TO eventschau_app;
```

- [ ] **Step 2: Sanity-check the SQL parses**

Run (offline lint of obvious syntax via a dry parse; if `psql` unavailable locally, skip and rely on review):

```bash
grep -q "CREATE DATABASE eventschau" db/provision.sql && echo "provision OK"
```

Expected output: `provision OK`

- [ ] **Step 3: Commit**

```bash
git add db/provision.sql
git commit -m "feat: db provisioning SQL for dedicated eventschau database"
```

---

### Task 5: Deploy workflow (rsync + compose + migrate)

**Files:**
- Create: `.github/workflows/deploy.yml`

**Interfaces:**
- Consumes: `docker-compose.yml` (Task 3), `Dockerfile` (Task 2), migrations (Task 1); GitHub secrets `DEPLOY_SSH_KEY`, optional `DEPLOY_HOST`/`DEPLOY_USER`/`DEPLOY_PORT`.
- Produces: automatic deploy on push to `main`.

- [ ] **Step 1: Create `.github/workflows/deploy.yml`**

```yaml
name: Deploy to production

# Push to main -> verify (test + build) -> rsync the checkout to the server and
# `docker compose up -d --build`. The server dir is a plain source tree (rsync
# target), NOT a git checkout, so we push code from the runner.
#
# Required repository secrets:
#   DEPLOY_SSH_KEY  — private key whose public half is in root@nyxcore.cloud authorized_keys
#   DEPLOY_HOST     — nyxcore.cloud   (optional; default nyxcore.cloud)
#   DEPLOY_USER     — root            (optional; default root)
#   DEPLOY_PORT     — 22              (optional; default 22)

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-eventschau-production
  cancel-in-progress: false

env:
  APP_DIR: /opt/e-ventschau
  COMPOSE_SERVICE: eventschau
  CONTAINER: e-ventschau

jobs:
  verify:
    name: Verify (test + build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      - run: npx prisma generate
      - run: npm test
      - run: npx next build
        env:
          NEXT_TELEMETRY_DISABLED: "1"
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DATABASE_URL_UNPOOLED: ${{ secrets.DATABASE_URL_UNPOOLED }}
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
          NEXTAUTH_URL: https://e-ventschau.nyxcore.cloud
          TENANT_SLUG: rd-e-ventschau

  deploy:
    name: Deploy (rsync + compose)
    needs: verify
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Configure SSH
        env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/id_deploy
          chmod 600 ~/.ssh/id_deploy
          ssh-keyscan -p "${DEPLOY_PORT:-22}" -H "${DEPLOY_HOST:-nyxcore.cloud}" >> ~/.ssh/known_hosts 2>/dev/null

      - name: Rsync source to server
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
        run: |
          set -euo pipefail
          # Additive (no --delete): preserves server-only files like .env.
          rsync -az \
            -e "ssh -i ~/.ssh/id_deploy -p ${DEPLOY_PORT:-22}" \
            --exclude=.git --exclude=node_modules --exclude=.next \
            --exclude=.env --exclude=.vercel --exclude=.DS_Store \
            --exclude=tsconfig.tsbuildinfo \
            ./ "${DEPLOY_USER:-root}@${DEPLOY_HOST:-nyxcore.cloud}:${APP_DIR}/"

      - name: Build, migrate, health-check
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/id_deploy -p "${DEPLOY_PORT:-22}" \
            "${DEPLOY_USER:-root}@${DEPLOY_HOST:-nyxcore.cloud}" \
            APP_DIR="$APP_DIR" SVC="$COMPOSE_SERVICE" CONTAINER="$CONTAINER" \
            'bash -seuo pipefail' <<'REMOTE'
          cd "$APP_DIR"
          docker compose up -d --build --remove-orphans
          # Apply Prisma migrations against the dedicated eventschau DB.
          docker compose exec -T "$SVC" ./node_modules/.bin/prisma migrate deploy \
            || echo "migrate skipped/failed — ensure the eventschau DB is provisioned & baselined"
          docker image prune -f >/dev/null 2>&1 || true
          for i in $(seq 1 20); do
            s="$(docker inspect -f '{{.State.Health.Status}}' "$CONTAINER" 2>/dev/null || echo missing)"
            echo "health attempt $i: $s"
            [ "$s" = "healthy" ] && exit 0
            sleep 6
          done
          echo "container did not become healthy in time" >&2
          docker logs --tail 50 "$CONTAINER" || true
          exit 1
          REMOTE
```

- [ ] **Step 2: Lint the workflow YAML**

Run (uses actionlint via Docker if available; otherwise a YAML parse):

```bash
docker run --rm -v "$PWD":/repo --workdir /repo rhysd/actionlint:latest -color .github/workflows/deploy.yml \
  || python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/deploy.yml')); print('yaml OK')"
```

Expected: actionlint passes (no errors) or `yaml OK`.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "feat: rsync+compose deploy workflow for nyxcore server"
```

---

### Task 6: Decommission Vercel config + update env docs

**Files:**
- Delete: `vercel.json`
- Delete: `.vercel/` (if tracked; otherwise add to `.gitignore`)
- Modify: `.env.example`

**Interfaces:**
- Produces: a repo with no Vercel build coupling and an `.env.example` matching the container deployment.

- [ ] **Step 1: Remove Vercel config**

```bash
git rm -f vercel.json
git rm -r --cached .vercel 2>/dev/null || true
```

- [ ] **Step 2: Ensure `.vercel` is ignored**

Append to `.gitignore` if not already present:

```
.vercel
```

Verify:

```bash
grep -q '^\.vercel' .gitignore && echo "gitignore OK"
```

Expected output: `gitignore OK`

- [ ] **Step 3: Update `.env.example` for self-hosted Postgres**

Replace the database + NextAuth section header comments and values so they describe the container deployment. The full new `.env.example`:

```
# Database (shared Postgres on the nyxcore server — dedicated `eventschau` DB)
DATABASE_URL=postgresql://eventschau_app:password@postgres:5432/eventschau
DATABASE_URL_UNPOOLED=postgresql://eventschau_app:password@postgres:5432/eventschau

# NextAuth
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=https://e-ventschau.nyxcore.cloud

# Tenant (overrides subdomain resolution — slug differs from subdomain)
TENANT_SLUG=rd-e-ventschau

# Public Site URL (for SEO, sitemap, metadata)
NEXT_PUBLIC_SITE_URL=https://e-ventschau.de
NEXT_PUBLIC_ROOT_DOMAIN=e-ventschau.nyxcore.cloud

# Cloudinary (media uploads — unchanged, media stays on Cloudinary)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# OpenAI (AI content features)
OPENAI_API_KEY=

# MiniRAG Widget (optional)
NEXT_PUBLIC_MINIRAG_ID=
NEXT_PUBLIC_MINIRAG_URL=
```

- [ ] **Step 4: Verify build still succeeds without vercel.json**

```bash
DATABASE_URL='postgresql://placeholder:placeholder@localhost:5432/placeholder' \
NEXTAUTH_SECRET='placeholder-build-secret' \
NEXTAUTH_URL='https://e-ventschau.nyxcore.cloud' \
TENANT_SLUG='rd-e-ventschau' \
npm run build
```

Expected: build succeeds.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: remove Vercel config, update .env.example for container deploy"
```

---

### Task 7: Cutover runbook (manual, one-time)

**Files:**
- Create: `docs/superpowers/runbooks/2026-06-23-eventschau-cutover.md`

**Interfaces:**
- Consumes: everything above + `db/provision.sql`. This task produces a documented manual procedure; no application code changes.

- [ ] **Step 1: Write the cutover runbook**

Create `docs/superpowers/runbooks/2026-06-23-eventschau-cutover.md` with the following content:

````markdown
# e-Ventschau Cutover Runbook (Vercel → nyxcore.cloud)

Prerequisites: merged `feat/container-migration-nyxcore` to `main`; GitHub
repo secrets set (`DEPLOY_SSH_KEY`, `DATABASE_URL`, `DATABASE_URL_UNPOOLED`,
`NEXTAUTH_SECRET`); SSH access to `root@46.224.105.254`.

## 1. Provision the database (on the server)

```bash
# Set a strong password in db/provision.sql first (replace CHANGE_ME_...).
cd /opt/nyxcore
docker compose exec -T postgres psql -U nyxcore -d postgres < /opt/e-ventschau/db/provision.sql
```

## 2. Dump Neon and restore into the eventschau DB

```bash
# From any machine with network access to Neon:
pg_dump --no-owner --no-acl "<NEON_UNPOOLED_URL>" | gzip > eventschau.sql.gz
scp eventschau.sql.gz root@46.224.105.254:/tmp/

# On the server, restore as eventschau_app into the eventschau DB:
gunzip -c /tmp/eventschau.sql.gz | \
  docker compose -f /opt/nyxcore/docker-compose.yml exec -T postgres \
  psql "postgresql://eventschau_app:<password>@localhost:5432/eventschau"
```

## 3. Baseline the Prisma migration (tables already exist from the dump)

Mark `0_init` as applied so `migrate deploy` doesn't try to re-create tables:

```bash
cd /opt/e-ventschau
docker compose exec -T eventschau ./node_modules/.bin/prisma migrate resolve --applied 0_init
```

## 4. Create the host .env

```bash
cat > /opt/e-ventschau/.env <<'EOF'
DATABASE_URL=postgresql://eventschau_app:<password>@postgres:5432/eventschau
DATABASE_URL_UNPOOLED=postgresql://eventschau_app:<password>@postgres:5432/eventschau
NEXTAUTH_SECRET=<from Vercel>
NEXTAUTH_URL=https://e-ventschau.nyxcore.cloud
TENANT_SLUG=rd-e-ventschau
NEXT_PUBLIC_SITE_URL=https://e-ventschau.de
NEXT_PUBLIC_ROOT_DOMAIN=e-ventschau.nyxcore.cloud
CLOUDINARY_CLOUD_NAME=<from Vercel>
CLOUDINARY_API_KEY=<from Vercel>
CLOUDINARY_API_SECRET=<from Vercel>
OPENAI_API_KEY=<from Vercel>
EOF
chmod 600 /opt/e-ventschau/.env
```

## 5. Trigger the deploy

Push to `main` (or run the "Deploy to production" workflow via
`workflow_dispatch`). It rsyncs, builds, runs `migrate deploy` (a no-op now
that `0_init` is resolved), and health-checks.

## 6. Row-count verification (source vs target)

For each key table compare counts on Neon vs the eventschau DB:

```bash
for t in "Page" "Artist" "Event" "Media" "Vendor" "Product" "Lead" "User"; do
  echo -n "$t neon=";   psql "<NEON_UNPOOLED_URL>" -tAc "SELECT count(*) FROM \"$t\";"
  echo -n "$t target="; docker compose -f /opt/nyxcore/docker-compose.yml exec -T postgres \
    psql "postgresql://eventschau_app:<password>@localhost:5432/eventschau" -tAc "SELECT count(*) FROM \"$t\";"
done
```

Counts must match.

## 7. Smoke test

1. `curl -I https://e-ventschau.nyxcore.cloud/` → 200, valid Let's Encrypt cert.
2. Open the homepage — correct tenant content renders.
3. A Cloudinary image loads (network tab shows `res.cloudinary.com`).
4. Admin login at `/admin/login` works.
5. Submit a lead (`POST /api/leads`) — row appears in the `Lead` table.

## 8. Repoint the daily backup

In the minicms repo, set the `DATABASE_URL_UNPOOLED` secret to the eventschau
URL (reachable from the GH runner only if the DB is exposed; if not, move the
backup to a server-side cron `pg_dump` — decide at cutover). The R2 bucket and
schedule stay as-is.

## Phase 2 (later, by Oliver): point e-ventschau.de here

1. Set the `e-ventschau.de` A-record → `46.224.105.254`.
2. Extend the Traefik router rule in `docker-compose.yml`:
   `Host(`e-ventschau.nyxcore.cloud`) || Host(`e-ventschau.de`) || Host(`www.e-ventschau.de`)`,
   commit, deploy (Let's Encrypt issues the new certs once DNS resolves).
3. Optionally switch `NEXTAUTH_URL` to `https://e-ventschau.de`.
4. Disable the Vercel project.
````

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/runbooks/2026-06-23-eventschau-cutover.md
git commit -m "docs: cutover runbook for eventschau Vercel->nyxcore migration"
```

---

## Self-Review

**Spec coverage:**
- Containerize (Dockerfile, compose) → Tasks 2, 3 ✓
- Traefik `e-ventschau.nyxcore.cloud` → Task 3 ✓
- Shared Postgres dedicated `eventschau` DB → Task 4 + Task 7 ✓
- Keep Cloudinary (no media changes) → respected (no task touches media) ✓
- `prisma migrate deploy` + baseline → Tasks 1, 5, 7 ✓
- CI/CD adjusted (deploy.yml) → Task 5; ci.yml unchanged (still valid) ✓
- R2 backup retained, repoint URL → Task 7 step 8 ✓
- Data migration (pg_dump/restore + verify) → Task 7 ✓
- `.env` host-only, rsync excludes → Tasks 5, 7 ✓
- `TENANT_SLUG`/`NEXTAUTH_URL` config → Tasks 5, 6, 7 ✓
- Vercel decommission → Task 6 ✓
- DNS Phase 2 documented → Task 7 ✓

**Placeholder scan:** No "TBD"/"handle edge cases"-style placeholders. `<password>`, `<from Vercel>`, `<NEON_UNPOOLED_URL>` are intentional runbook secrets supplied at cutover, not code gaps.

**Type/name consistency:** Service name `eventschau`, container `e-ventschau`, image `e-ventschau:latest`, DB `eventschau`, role `eventschau_app`, migration `0_init`, deploy path `/opt/e-ventschau`, migrate command `./node_modules/.bin/prisma migrate deploy` — consistent across Tasks 2, 3, 5, 7.
